language: ruby
rvm:
  - 2.1

install: gem install jekyll -v 2.4.0 && gem install s3_website jekyll-redirect-from jekyll-paginate jekyll-minify-html jekyll-seo-tag jekyll-sitemap

script: jekyll build

after_success: s3_website push
